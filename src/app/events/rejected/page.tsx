import EventsListPage from '@/components/EventsListPage';
export default function RejectedPage() {
  return <EventsListPage status="rejected" title="Rejected events" emptyMsg="No rejected events"/>;
}
